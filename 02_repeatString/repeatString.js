const repeatString = repeat('roshay',3);
repeat(string, num)
{
    var times;
    var word;
    for(i=1;i<=times;i++)
    {
        var finalstring = repeatString + word;
    }
    return finalstring;
}
console.log(repeatString);

// Do not edit below this line
module.exports = repeatString;
